"use client";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { useCitizenDashboardMetricsQuery } from "@/hooks";
export default function CitizenDashboardPage(){const q=useCitizenDashboardMetricsQuery();const m=q.data;const cards=[['Total Households',m?.total_households],['Total Citizens',m?.total_registered_citizens],['Active Citizens',m?.active_citizens],['Pending Verification',m?.pending_verifications],['Duplicate Alerts',m?.duplicate_alerts],['Suspended',m?.suspended_citizens]];return <div className="space-y-6"><div><h1 className="text-2xl font-bold">Citizen Dashboard</h1><p className="text-muted-foreground">Citizen administration metrics.</p></div><div className="grid gap-4 md:grid-cols-3">{cards.map(([label,value])=><Card key={String(label)}><CardHeader><CardTitle className="text-sm text-muted-foreground">{label}</CardTitle></CardHeader><CardContent><p className="text-3xl font-bold">{Number(value??0).toLocaleString()}</p></CardContent></Card>)}</div></div>}
