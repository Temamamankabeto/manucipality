"use client";
import { useState } from "react";
import { CheckCheck, Loader2, Trash2 } from "lucide-react";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { useDeleteNotificationMutation, useMarkAllNotificationsReadMutation, useMarkNotificationReadMutation, useNotificationsQuery } from "@/hooks";
export default function NotificationsPage(){const [page,setPage]=useState(1);const q=useNotificationsQuery({page,per_page:10});const mark=useMarkNotificationReadMutation();const all=useMarkAllNotificationsReadMutation();const del=useDeleteNotificationMutation();const rows=q.data?.data??[];return <div className="space-y-6"><div className="flex justify-between"><div><h1 className="text-2xl font-bold">Notifications</h1><p className="text-muted-foreground">In-app citizen management alerts.</p></div><Button variant="outline" onClick={()=>all.mutate()}><CheckCheck className="mr-2 h-4 w-4"/>Mark all read</Button></div><Card><CardHeader><CardTitle>Inbox</CardTitle></CardHeader><CardContent className="space-y-3">{q.isLoading?<div className="flex justify-center py-10"><Loader2 className="mr-2 h-4 w-4 animate-spin"/>Loading...</div>:rows.length===0?<p className="py-8 text-center text-muted-foreground">No notifications.</p>:rows.map(n=><div key={n.id} className="flex justify-between rounded-lg border p-3"><div><p className="font-medium">{n.title}</p><p className="text-sm text-muted-foreground">{n.message}</p><p className="text-xs text-muted-foreground">{n.created_at ? new Date(n.created_at).toLocaleString() : ""}</p></div><div className="flex gap-2"><Button size="sm" variant="outline" onClick={()=>mark.mutate(n.id)}>Read</Button><Button size="sm" variant="destructive" onClick={()=>del.mutate(n.id)}><Trash2 className="h-4 w-4"/></Button></div></div>)}</CardContent></Card></div>}
